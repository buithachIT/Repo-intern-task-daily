
import { ReactNode } from 'react';

function BlogLayout({ children }: { children: ReactNode }) {
    return (
        <>
            {children}
        </>
    );
}

export default BlogLayout;